<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.marek.myapplication" >

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Sportowe"
            android:label="@string/title_activity_sportowe"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>
        <activity
            android:name=".Kulturalne"
            android:label="@string/title_activity_kulturalne"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>
        <activity
            android:name=".Klubowe"
            android:label="@string/title_activity_klubowe"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>
        <activity
            android:name=".Inne"
            android:label="@string/title_activity_inne"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>
        <activity
            android:name=".Wybor"
            android:label="@string/title_activity_wybor"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" >
        </activity>
        <activity
            android:name=".KlubKazik"
            android:label="@string/title_activity_klub_kazik1"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>
        <activity
            android:name=".KlubSilence"
            android:label="@string/title_activity_klub_silence"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>
        <activity
            android:name=".KlubCzekolada"
            android:label="@string/title_activity_klub_czekolada"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>
        <activity
            android:name=".KlubShine"
            android:label="@string/title_activity_klub_shine"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>
        <activity
            android:name=".Miejsce"
            android:label="@string/title_activity_miejsce"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.marek.myapplication.MainActivity" />
        </activity>
    </application>

</manifest>
